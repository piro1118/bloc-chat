(function() {
    function NewRoomCtrl(Room, $uibModalInstance) {

    };

    angular
        .module('blocChat')
        .controller('NewRoomCtrl', ['Room', '$uibModalInstance', NewRoomCtrl]);
})();
